package projectCP.codeeditor;

public enum CodeTextType {
    TEXT_INSERTED,
    TEXT_DELETED,
    CURSOR_MOVED,
    FILE_OPENED,
    FILE_CLOSED,
    TEXT_REPLACED,
    SELECTION_CHANGED,
    FILE_SAVED,
    UNDO,
    REDO
}
